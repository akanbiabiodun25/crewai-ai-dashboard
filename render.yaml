services:
  - type: web
    name: crewai-ai-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: OPENROUTER_API_KEY
        fromEnvVar: OPENROUTER_API_KEY
